#grid 
    %table
        %tr.row
            %th 
                = link_to "Col 1", move_path(col: 1, id: board.id, game_id: game.id), class: 'button', method: :post, remote: true 

            %th 
                = link_to "Col 2", move_path(col: 2, id: board.id, game_id: game.id), class: 'button', method: :post, remote: true
            %th 
                = link_to "Col 3", move_path(col: 3, id: board.id, game_id: game.id), class: 'button', method: :post, remote: true                        
            %th 
                = link_to "Col 4", move_path(col: 4, id: board.id, game_id: game.id), class: 'button', method: :post, remote: true                        
            %th 
                = link_to "Col 5", move_path(col: 5, id: board.id, game_id: game.id), class: 'button', method: :post, remote: true                        
            %th 
                = link_to "Col 6", move_path(col: 6, id: board.id, game_id: game.id), class: 'button', method: :post, remote: true                        
            %th 
                = link_to "Col 7", move_path(col: 7, id: board.id, game_id: game.id), class: 'button', method: :post, remote: true  
            
        
        - grid.each do |row|
            %tr.row
                - row.each do |cell|
                    %td{class: "#{cell.value}", id: "cell"}